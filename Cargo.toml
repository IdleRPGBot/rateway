[package]
name = "rateway"
version = "0.1.0"
authors = ["Jens Reidel <adrian@travitia.xyz>"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
[dependencies]
twilight-gateway = { branch = "raw-command", default-features = false, features = [
    "rustls",
    "simd-json",
    "simd-zlib"
], git = "https://github.com/Gelbpunkt/twilight" }
twilight-model = { branch = "raw-command", git = "https://github.com/Gelbpunkt/twilight" }
twilight-http = { branch = "raw-command", default-features = false, features = [
    "rustls",
    "simd-json"
], git = "https://github.com/Gelbpunkt/twilight" }
twilight-cache-inmemory = { branch = "raw-command", git = "https://github.com/Gelbpunkt/twilight" }
tokio-amqp = { version = "0.1", default-features = false }
lapin = { version = "1.2", default-features = false }
futures = "0.3"
tokio = { version = "0.2", features = ["macros", "rt-core", "rt-threaded"] }
serde = { version = "1", features = ["derive"] }
simd-json = { default-features = false, features = [
    "serde_impl",
    "swar-number-parsing"
], version = "0.3" }
env_logger = "0.7"
log = "0.4"
